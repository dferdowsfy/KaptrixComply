# Development defaults — safe to commit. No secrets here.
# Secrets (Supabase keys, API keys) go in .env.local which is gitignored.
NEXT_PUBLIC_APP_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000
