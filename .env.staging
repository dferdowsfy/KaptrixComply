# Staging defaults — safe to commit. No secrets here.
# On Vercel: these are overridden by "Preview" environment variables.
# Locally: copy to .env.local and fill in staging Supabase credentials.
NEXT_PUBLIC_APP_ENV=staging
NEXT_PUBLIC_APP_URL=https://staging.kaptrix.com
