# Production defaults — safe to commit. No secrets here.
# On Vercel: these are overridden by "Production" environment variables.
NEXT_PUBLIC_APP_ENV=production
NEXT_PUBLIC_APP_URL=https://kaptrix.com
